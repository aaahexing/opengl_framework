file(GLOB_RECURSE CORE_HEADERS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} FOLLOW_SYMLINKS ./*.h)
file(GLOB_RECURSE CORE_SOURCES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} FOLLOW_SYMLINKS ./*.cpp)

QT4_WRAP_CPP(QT_MOC_CORE ${CORE_HEADERS})
add_library(core ${QT_MOC_CORE} ${CORE_SOURCES})
